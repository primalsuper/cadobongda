
---
const { post } = Astro.props;
---

<article class="card">
 <img src={post.image} alt={post.title} loading="lazy" />
 <div class="content">
  <small>{post.category}</small>
  <h2>{post.title}</h2>
  <p>{post.excerpt}</p>
  <a href={`/artikel/${post.slug}/`}>Read more</a>
 </div>
</article>
